import React from 'react';

//Componente para trabajar Productos Mínimos: Artículos
const ArticulosMinimos = ({ subcategory }) => {
  return <div>Productos mínimos: Línea - {subcategory}</div>;
};

export default ArticulosMinimos;